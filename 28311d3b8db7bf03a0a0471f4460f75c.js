if ('0' != '0') {
            var domainInfo = 'xm';
            if (domainInfo != null && domainInfo != '') {
                $.cookie('bzw315_bcitycode', domainInfo, { expires: 7, domain: "bzw315.com", path: "/" });
            }
        }