function initITAcontext(window) {
            var context = {};
            context.flags = {
              
              
              
            };
            window.__LS_ITA_CONTEXT__ = context;
          }
          initITAcontext(window);